apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-demo-web
  namespace: python-demo
  labels: { app.kubernetes.io/name: python-demo, app.kubernetes.io/component: web }
spec:
  # no `replicas` here on purpose: the HPA in hpa.yaml owns the count
  selector:
    matchLabels: { app.kubernetes.io/name: python-demo, app.kubernetes.io/component: web }
  template:
    metadata:
      labels: { app.kubernetes.io/name: python-demo, app.kubernetes.io/component: web }
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: web
          image: python-demo-web:1.0.0
          imagePullPolicy: IfNotPresent
          ports: [{ name: http, containerPort: 8080 }]
          env:
            - { name: APP_NAME, value: python-demo }
            - { name: SERVICE_NAME, value: web }
            - { name: APP_VERSION, value: "1.0.0" }
          envFrom:
            - configMapRef: { name: python-demo-web-config }
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 2, periodSeconds: 5 }
          livenessProbe:  { httpGet: { path: /health, port: http }, initialDelaySeconds: 5, periodSeconds: 10 }
          resources:
            requests: { cpu: 25m, memory: 32Mi }
            limits:   { cpu: 250m, memory: 128Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts: [{ name: tmp, mountPath: /tmp }]
      volumes: [{ name: tmp, emptyDir: {} }]
---
apiVersion: v1
kind: Service
metadata:
  name: python-demo-web
  namespace: python-demo
spec:
  type: ClusterIP            # LoadBalancer on AKS to get a public IP
  selector: { app.kubernetes.io/name: python-demo, app.kubernetes.io/component: web }
  ports: [{ name: http, port: 80, targetPort: http }]
