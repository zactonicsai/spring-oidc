# Copy to azure.ini / local.ini (both are git-ignored). scripts/azure/11-run-ansible.sh and
# terraform generate these for you.
[app_hosts]
# an Azure VM (or any Ubuntu box) over SSH:
# vm-python-demo ansible_host=20.1.2.3 ansible_user=azureuser
# a local docker "host" container (terraform/local-docker) via the docker connection plugin:
# python-demo-host ansible_connection=community.docker.docker

[app_hosts:vars]
# image_registry=myacr.azurecr.io      # empty = local images
