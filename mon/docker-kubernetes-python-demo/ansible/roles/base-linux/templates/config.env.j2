# generated by ansible (role base-linux) from app.config.yaml hosts.settings
APP_NAME={{ app_name }}
APP_VERSION={{ app_version }}
{% for k, v in (hostcfg.settings | default({})).items() | sort %}
{{ k }}={{ v }}
{% endfor %}
