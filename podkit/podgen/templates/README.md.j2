# {{ name }} on {{ provider_title }}

Generated by podgen from `{{ source_file }}`. Do not edit; change the PodConfig and regenerate:

```bash
python -m podgen generate {{ source_file }} --provider {{ provider }} --force
```

| File | Purpose |
| --- | --- |
| `deploy.sh` / `destroy.sh` | kubectl path: secrets from {{ secret_store }}, ServiceAccount, workload, rollout, configuration |
| `configure.sh` | {{ configure.method }} configuration{% if configure.method != 'none' %} (`{{ configure.target }}`, {{ configure.phase }}-deploy) run from the command pod{% endif %} |
| `k8s/` | ServiceAccount and workload manifests (Deployment, Service, NetworkPolicy, ...) |
| `terraform/` | Root module using `terraform/modules/{{ provider }}` and `terraform/modules/pod` |
| `pod.env` | Cluster settings shared by the scripts; set `IDENTITY_ID` from `terraform output identity` |
{% if configure.method != 'none' %}
| `ansible/vars.yml`, `configure.env` | Variables for the Ansible playbook / shell script |
{% endif %}

```bash
../../../scripts/cluster-login.sh -p {{ provider }} -c {{ cloud.clusterName }} {% if provider == 'aws' %}-r {{ cloud.region }}{% elif provider == 'azure' %}-g {{ cloud.resourceGroup }}{% else %}-P {{ cloud.project }} -l {{ cloud.location }}{% endif %}

./deploy.sh                                   # kubectl path
../../../scripts/deploy.sh -m terraform .     # or: Terraform path (creates the cloud identity too)
./destroy.sh
```
